---
name: open_firefox
description: Opens the Firefox browser on the user's system.
---
# Open Firefox

This skill allows the agent to open the Firefox browser on the user's Windows system.

## Instructions
1. Use the `run_command` tool.
2. Run the command `start firefox`.
3. Verify if the command executed successfully by checking the output or command status.
